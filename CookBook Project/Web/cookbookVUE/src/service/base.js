const urlBase = "http://localhost:5250/api"

const mealUrl = urlBase + "/meal"
const meassureUrl = urlBase + "/measure"
const mealTypeUrl = urlBase + "/mealtype"
const userUrl = urlBase + "/user"
const favoriteUrl = urlBase + "/favorite"
const tokenUrl = urlBase + "/token"

export default {urlBase, mealUrl, meassureUrl, mealTypeUrl, userUrl, favoriteUrl, tokenUrl}